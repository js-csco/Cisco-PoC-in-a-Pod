apiVersion: v1
kind: Service
metadata:
  name: kubectl-mcp
  namespace: piap
  labels:
    app: kubectl-mcp
spec:
  type: NodePort
  selector:
    app: kubectl-mcp
  ports:
    - port: 8811
      targetPort: 8811
      nodePort: 32811
      protocol: TCP
