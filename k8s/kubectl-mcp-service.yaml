apiVersion: v1
kind: Service
metadata:
  name: kubectl-mcp
  namespace: piap
  labels:
    app: kubectl-mcp
spec:
  type: NodePort
  selector:
    app: kubectl-mcp
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30050
      protocol: TCP
