apiVersion: v1
kind: Service
metadata:
  labels:
    app: poc-dashboard
  name: poc-dashboard
  namespace: piap
spec:
  type: NodePort
  selector:
    app: poc-dashboard
  ports:
    - name: "poc-dashboard"
      port: 9200
      targetPort: 8080
      nodePort: 30200
status:
  loadBalancer: {}
