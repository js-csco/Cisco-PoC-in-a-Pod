Cisco Secure Access - poc in a pod:

Prerequisites:
- prepare a Linux Ubuntu 24.04.x LTS VM
- update & install required dependencies

commands to run on VM:
command to start pod: podman-compose up -d --build
command to stop pod: podman-compose down

next steps:
- youtube video